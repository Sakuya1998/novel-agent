global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/novel-agent-alerts.yml

scrape_configs:
  # 将 api 替换为实际 API 服务名或反向代理地址。
  - job_name: novel-agent
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
