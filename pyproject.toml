[project]
name = "novel-agent"
version = "2.0.0"
description = "Multi-Agent 小说创作系统 【墨笔】 — LangGraph + ChromaDB 的人机协作长篇创作平台"
readme = "README.md"
requires-python = ">=3.14"
license = { text = "MIT" }
dependencies = [
    "langgraph>=1.2",
    "langgraph-checkpoint-sqlite>=3.0",
    "langchain>=1.0",
    "langchain-openai>=1.0",
    "langchain-anthropic>=1.0",
    "langchain-chroma>=1.0",
    "chromadb>=1.0",
    "cryptography>=46.0",
    "fastapi>=0.140",
    "python-multipart>=0.0.20",
    "uvicorn>=0.32",
    "python-dotenv>=1.0.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
    "ruff>=0.4",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["agents", "graph", "memory", "tools", "prompts", "models", "api"]
py-modules = ["config", "security"]

# ---------------- Ruff ----------------
[tool.ruff]
line-length = 120
target-version = "py314"
exclude = [".venv", "data", "output", "memory"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
    "SIM", # flake8-simplify
]
ignore = [
    "B008", # FastAPI 依赖注入风格需要在参数中调用函数
]

[tool.ruff.lint.isort]
known-first-party = ["agents", "graph", "memory", "tools", "prompts", "models", "api", "config"]

# ---------------- Pytest ----------------
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-q"
